import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  outDir: 'dist',  // Asegúrate de que los archivos generados se colocan en el directorio 'dist'
  // Puedes agregar más configuraciones aquí si las necesitas
});
